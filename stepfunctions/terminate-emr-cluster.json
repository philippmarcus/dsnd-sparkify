{
    "Comment": "Terminates a given EMR cluster. Expects a field ClusterId as input parameter.",
    "StartAt": "Terminate EMR cluster",
    "States": {
      "Terminate EMR cluster": {
        "Type": "Task",
        "Resource": "arn:aws:states:::elasticmapreduce:terminateCluster.sync",
        "Parameters": {
          "ClusterId": "$.ClusterId"
        },
        "End": true
      }
    }
  }